CMakeLists.txt.user
CMakeCache.txt
CMakeFiles
CMakeScripts
Testing
Makefile
cmake_install.cmake
install_manifest.txt
compile_commands.json
CTestTestfile.cmake
_deps

CMakeSettings.json

.vs/
.vscode/
out/
build/
# A second tree for another compiler (build-clang, …). The sanitizer folders, by
# contrast, sit under build/ and are covered already.
build-*/
.clangd/
# The .clangd file (not the directory) balances the flag drift on the
# infrastructure headers. Local for now: let it prove itself first, then decide
# whether it belongs versioned.
.clangd
.cache/

compile_commands.json

CMakePresets.json

/gh_act_token.txt

.claude/settings.json

TODO.txt
TODO.md
# Real data (privacy), never belongs in the repo
/Aufenthaltsdaten*
