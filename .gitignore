CMakeLists.txt.user
CMakeCache.txt
CMakeFiles
CMakeScripts
Testing
Makefile
cmake_install.cmake
install_manifest.txt
compile_commands.json
CTestTestfile.cmake
_deps

CMakeSettings.json

.vs/
.vscode/
out/
build/
# Zweiter Baum für einen anderen Compiler (build-clang, …). Die Sanitizer-
# Ordner liegen dagegen unter build/ und sind schon abgedeckt.
build-*/
.clangd/
# Die .clangd-Datei (nicht das Verzeichnis) gleicht die Flag-Drift bei den
# infrastructure-Headern aus. Vorerst lokal: erst bewähren lassen, dann
# entscheiden, ob sie versioniert gehört.
.clangd
.cache/

compile_commands.json

CMakePresets.json

/gh_act_token.txt

.claude/settings.json

TODO.txt
TODO.md
# Echtdaten (Datenschutz), gehören nie ins Repo
/Aufenthaltsdaten*
