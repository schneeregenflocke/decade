# Qt6::Platform writes -mno-direct-extern-access for GCC, and clang rejects it
# as an unknown argument, so clangd would mark line 1 of every file. A no-op
# when build/ is a clang tree. Issue #87.
CompileFlags:
  Remove: [-mno-direct-extern-access]
